export async function runOneShot(createService,run){let service=createService();try{await service.initialize();return await run(service)}finally{await service.destroy()}}export function splitOptions(options){if(!options){return{paddle:{},perCall:{}}}const{model,detection,recognition,debugging,session,processing,...perCall}=options;return{paddle:{model,detection,recognition,debugging,session,processing},perCall}}