import{mirrorUrl}from"./model-catalogue.js";export function deepMerge(target,...sources){if(!sources.length)return target;let source=sources.shift();if(isObject(target)&&isObject(source)){for(let key in source){if(Object.prototype.hasOwnProperty.call(source,key)){if(key==="__proto__"||key==="constructor"||key==="prototype"){continue}let sourceValue=source[key];let targetValue=target[key];if(isObject(sourceValue)){if(!targetValue||!isObject(targetValue)){target[key]={}}deepMerge(target[key],sourceValue)}else if(sourceValue!==undefined){target[key]=sourceValue}}}}return deepMerge(target,...sources)}export function levenshteinDistance(a,b){let m=a.length;let n=b.length;if(m===0)return n;if(n===0)return m;let prev=new Uint16Array(n+1);let curr=new Uint16Array(n+1);for(let j=0;j<=n;j++)prev[j]=j;for(let i=1;i<=m;i++){curr[0]=i;for(let j=1;j<=n;j++){let cost=a[i-1]===b[j-1]?0:1;curr[j]=Math.min((curr[j-1]??0)+1,(prev[j]??0)+1,(prev[j-1]??0)+cost)}let tmp=prev;prev=curr;curr=tmp}return prev[n]??0}let MAX_RETRY_AFTER_MS=60000;export function parseRetryAfter(header,now){if(!header)return null;let trimmed=header.trim();if(trimmed==="")return null;let seconds=Number(trimmed);if(Number.isFinite(seconds)){return seconds<=0?0:Math.min(seconds*1000,MAX_RETRY_AFTER_MS)}let at=Date.parse(trimmed);if(Number.isNaN(at))return null;return Math.min(Math.max(at-now,0),MAX_RETRY_AFTER_MS)}function backoffMs(attempt){let base=500*2**attempt;return base+Math.random()*base}export async function fetchArrayBufferWithRetry(url,options={}){const{timeoutMs=300000,retries=2,fallbackUrl=mirrorUrl(url)}=options;let lastError;for(let attempt=0;attempt<=retries;attempt++){let retryAfterMs=null;try{let response=await fetch(url,{signal:AbortSignal.timeout(timeoutMs),referrerPolicy:"no-referrer"});if(!response.ok){retryAfterMs=parseRetryAfter(response.headers.get("retry-after"),Date.now());throw new Error(`HTTP ${response.status} ${response.statusText}`)}return await response.arrayBuffer()}catch(error){lastError=error;if(attempt<retries){await new Promise((resolve)=>setTimeout(resolve,retryAfterMs??backoffMs(attempt)))}}}let primaryFailure=new Error(`Failed to fetch ${url} after ${retries+1} attempt(s): ${String(lastError)}`);if(!fallbackUrl)throw primaryFailure;console.warn(`[ppu-paddle-ocr] ${primaryFailure.message}
  retrying on ${fallbackUrl}`);try{return await fetchArrayBufferWithRetry(fallbackUrl,{timeoutMs,retries,fallbackUrl:null})}catch(mirrorError){throw new Error(`${primaryFailure.message}; mirror also failed: ${String(mirrorError)}`,{cause:primaryFailure})}}export function parseDictionary(source){let content=typeof source==="string"?source:new TextDecoder("utf-8").decode(source);return content.split(/\r?\n/)}export function isObject(item){return item!==null&&typeof item==="object"&&!Array.isArray(item)&&!(item instanceof Date)&&!(item instanceof RegExp)&&!(item instanceof ArrayBuffer)&&!ArrayBuffer.isView(item)}